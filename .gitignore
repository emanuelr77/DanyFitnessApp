# Carpeta de backups locales — no se versiona
BK/

# Arte fuente en alta resolución (no se usa en runtime; se mantiene local)
LogoCompleto.png

# Archivos de sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Emblema fuente (ya va incrustado en index.html + icon.png) — solo local
header-emblema-color-512.png
